export default { apiVersion: 1, panels: [{ id: 'guide', title: 'Plugin Guide', scope: 'global', mount(root) { const h = document.createElement('h2'); h.textContent = 'Build a feature. Share a plugin.'; const p = document.createElement('p'); p.textContent = 'Use the public SDK to add panels, actions, settings and app recipes. Open the user and agent guides in this plugin’s details for the complete steps.'; const pre = document.createElement('pre'); pre.textContent = 'mobile-builder plugin new ./my-plugin\nmobile-builder plugin validate ./my-plugin\nmobile-builder plugin pack ./my-plugin'; root.append(h, p, pre); return () => root.replaceChildren(); } }] };
