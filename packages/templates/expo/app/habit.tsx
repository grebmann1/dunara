import { View } from 'react-native';
import { Button, Card, Input, Row, Screen, Type, t } from '../src/ui';
import { useHabit } from '../src/state';
export default function Habit() {
  const { completed, setCompleted, note, setNote } = useHabit();
  return <Screen><View style={{ gap: 8 }}><Type kind="label" color={t.muted}>MINDFUL MOMENTS · 5 MIN</Type><Type kind="title">Come back{ '\n' }to your breath.</Type><Type color={t.muted}>A small pause can change your whole day.</Type></View><Card><View style={{ alignItems: 'center', paddingVertical: 22 }}><View style={{ width: 190, height: 190, borderRadius: 100, borderWidth: 1, borderColor: t.border, padding: 20 }}><View style={{ flex: 1, borderRadius: 100, borderWidth: 1, borderColor: t.accent, justifyContent: 'center', alignItems: 'center', gap: 4 }}><Type kind="metric">4 · 4</Type><Type kind="label" color={t.muted}>INHALE · EXHALE</Type></View></View></View><Type center color={t.muted}>Find a comfortable seat.{ '\n' }Relax your shoulders. You’re here.</Type></Card><View style={{ gap: 18 }}>{['Settle into a comfortable position', 'Breathe in gently for four counts', 'Breathe out slowly for four counts'].map((step, index) => <Row key={step}><View style={{ width: 32, height: 32, borderRadius: 16, backgroundColor: t.surface, alignItems: 'center', justifyContent: 'center' }}><Type color={t.accent}>{index + 1}</Type></View><View style={{ flex: 1 }}><Type>{step}</Type></View></Row>)}</View><Input label="TODAY’S INTENTION" value={note} onChangeText={setNote} /><Button label={completed ? 'Completed — undo' : 'Mark ritual complete'} onPress={() => setCompleted(!completed)} /><Type center color={t.muted}>{completed ? 'You made a little room for yourself. Well done.' : 'No timer. No pressure. Your own pace.'}</Type></Screen>;
}
