import { View } from 'react-native';
import { router } from 'expo-router';
import { Button, Card, Progress, Row, Screen, Type, t } from '../src/ui';
import { useHabit } from '../src/state';
export default function Today() {
  const { completed } = useHabit();
  return <Screen><View style={{ gap: 8 }}><Type kind="label" color={t.muted}>YOUR DAILY RESET</Type><Type kind="title">Small steps.{ '\n' }A softer kind of day.</Type><Type color={t.muted}>You don’t need to do it all. Just begin.</Type></View><Card accent><Row><Type kind="label" color={t.onAccent}>MAKE SPACE FOR YOU</Type><Type color={t.onAccent}>01 / 03</Type></Row><View style={{ height: 112, alignItems: 'center', justifyContent: 'center' }}><View style={{ width: 106, height: 106, borderRadius: 60, borderWidth: 1, borderColor: t.onAccent, opacity: .55, alignItems: 'center', justifyContent: 'center' }}><View style={{ width: 72, height: 72, borderRadius: 40, borderWidth: 1, borderColor: t.onAccent, alignItems: 'center', justifyContent: 'center' }}><View style={{ width: 38, height: 38, borderRadius: 25, backgroundColor: t.onAccent }} /></View></View></View><Type kind="heading" color={t.onAccent}>A moment of stillness</Type><Type color={t.onAccent}>Five minutes to breathe, reconnect,{ '\n' }and start again.</Type></Card><Row><Type kind="heading">Your little rituals</Type><Type color={t.muted}>{completed ? '2' : '1'} of 3</Type></Row><Card><Row><View style={{ flex: 1, gap: 4 }}><Type kind="heading">Morning breathing</Type><Type color={t.muted}>5 min · Mindful moments</Type></View><Type color={t.accent}>{completed ? '✓' : '○'}</Type></Row><Progress value={completed ? 2 / 3 : 1 / 3} label="Daily rituals completed" /><Button label={completed ? 'Revisit your ritual' : 'Begin your ritual'} onPress={() => router.push('/habit')} /></Card><Row><Type color={t.muted}>A glass of water</Type><Type color={t.accent}>✓ Done</Type></Row><Row><Type color={t.muted}>An evening walk</Type><Type color={t.muted}>Later today</Type></Row><Type kind="label" center color={t.muted}>PROGRESS, NOT PERFECTION.</Type></Screen>;
}
