/** Shared brief for explicit Create and build or a staged, editable request. */
export function projectBuildPrompt(name: string, brief: string) {
  return `Build the first version of ${name}.\n\n${brief.trim()}\n\nMake a complete, distinctive app that follows this idea and its visual direction. Replace unrelated starter content across every tab, empty state and detail screen. Establish one consistent palette, typography system and artwork style; preserve the requested illustration technique and level of realism. Use real image references when available. Image generation still follows the normal request review.\n\nMake the main action work, with useful feedback, empty states and recoverable errors. Persist user-created records and preferences across reopening on web and native; do not present seeded examples as a connected backend. Keep the hero, primary action and useful supporting content readable at 375×812 and 430×932, with safe areas and reachable bottom navigation.\n\nStart the preview when execution is available. Check TypeScript, open every route, capture both phone sizes, inspect the images and browser diagnostics, exercise the primary action and verify its data survives a reload. Report each check actually performed, its evidence and anything unverified. A completed build turn is not proof of physical-device behavior. Tell me what I can try next.`;
}
